// Paste your Firebase project config here to make the guestbook and visitor
// counter shared across everyone (Firebase console -> Project settings ->
// General -> Your apps -> SDK setup and configuration).
//
// Until this is filled in, the site falls back to a local-only guestbook
// (stored in each visitor's own browser) so nothing looks broken.
window.FIREBASE_CONFIG = {
  apiKey: "",
  authDomain: "",
  projectId: "",
  storageBucket: "",
  messagingSenderId: "",
  appId: "",
};
